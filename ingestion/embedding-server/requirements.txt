# -- Future Optional Local Embedding Model Server ----------------------------
# Current implementation uses OpenAI embeddings. Install this file only when working on the future local/open-source embedding server.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Settings & Validation ─────────────────────────────────────────────────────
pydantic

# ── ML / NLP (required for Clinical-BERT embedding inference) ─────────────────
# transformers==4.46.3
# torch==2.7.0
numpy